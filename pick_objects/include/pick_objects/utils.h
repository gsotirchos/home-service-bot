namespace pick_objects {
    enum RobotState {
        FINISHED = 0,
        FAILED = 1,
        MOVING = 2
    };
}  // namespace pick_objects

namespace add_markers {
    enum MarkerState {
        FINISHED = 0,
        PICKUP = 1,
        DROPOFF = 2
    };
}  // namespace add_markers
